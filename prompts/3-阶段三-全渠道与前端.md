---
📢 Codex 客户端使用说明：
1. ✅ 前置条件：阶段一 + 阶段二已全部完成并通过验收
2. ✅ 确认左侧文件树包含 crawlers/sources/iguopin_crawler.py 和 job51_crawler.py
3. ✅ 确认 matching/ 目录已存在
4. 将本文件全部内容复制粘贴到 Codex 对话框后发送
5. 回复被截断 → 直接回复「请继续」
---

# Phase 3 — 全渠道覆盖 + 前端完善

> **目标**：补全剩余 6 个渠道爬虫 + 完善前端所有页面 + 采集看板
> **前置条件**：Phase 1 和 Phase 2 已完成
> **注意**：如果一次生成内容太多，可回复「只生成 Task 3.1-3.3」分批次生成

---

## Task 3.1 — 国资委爬虫

创建 **`crawlers/sources/sasac_crawler.py`**:
- 继承 BaseCrawler，source_name = "sasac"
- 目标: `http://www.sasac.gov.cn` 招聘公告栏目
- 实现 fetch_list(): 访问招聘公告列表页 → 解析公告标题/链接 → 翻页 2-3 页
- 进入公告详情页提取岗位信息：从正文中拆解岗位（通常是表格或段落列表）
- 如果无法确定真实页面结构，生成 Mock 数据版本（10条），并用 # TODO 注释标注需要替换的选择器。Mock 仅在 `ALLOW_MOCK_DATA=true` 时启用，默认关闭：
  ```python
  # TODO: 选择器需根据实际页面结构调整
  # 假设公告列表在 <div class="list_news"> 下的 <li>
  ```
- parse_item() 返回 RawJobItem

更新 **`crawlers/__init__.py`** 的 CRAWLERS 字典，注册 "sasac": SasacCrawler()

---

## Task 3.2 — 人社部爬虫

创建 **`crawlers/sources/mohrss_crawler.py`**:
- 继承 BaseCrawler，source_name = "mohrss"
- 目标: `http://www.mohrss.gov.cn` 就业招聘频道
- 优先尝试寻找 RSS 源（如 /rss.xml），有则用 feedparser/requests 解析（feedparser 如没安装则用 xml.etree）
- 无 RSS 则用 HTML 爬虫解析列表页
- 事业单位/公共就业岗位，注意政务信息格式
- Mock 版本兜底；仅在 `ALLOW_MOCK_DATA=true` 时启用，默认关闭

更新 CRAWLERS 注册 "mohrss"。

---

## Task 3.3 — 企业官网定向爬虫

创建 **`crawlers/sources/official_crawler.py`**:
- 继承 BaseCrawler，source_name = "official"
- 设计模式：公司列表可配置化 + 适配器模式
- fetch_list() 逻辑：
  ```
  1. 从 config/sources.yaml 读取 official.companies 列表
  2. 遍历每家公司，调用对应 parser 函数
  3. 合并所有公司的岗位结果
  ```
- 提供 3 个样例公司 parser：
  - `_parse_tencent(url)`: 腾讯招聘（返回 Mock 数据 + 注释说明页面结构假设）
  - `_parse_alibaba(url)`: 阿里招聘（Mock）
  - `_parse_bytedance(url)`: 字节招聘（Mock）
 - 支持 Playwright 处理 JS 渲染页面；异步爬虫必须使用 `async_playwright()`，不得在事件循环中调用 `sync_playwright()`

更新 **`config/sources.yaml`**，追加 official 配置：
```yaml
official:
  enabled: true
  schedule: "0 */4 * * *"
  companies:
    - name: "腾讯"
      url: "https://join.tencent.com"
      parser: "tencent"
    - name: "阿里巴巴"
      url: "https://talent.alibaba.com"
      parser: "alibaba"
    - name: "字节跳动"
      url: "https://jobs.bytedance.com"
      parser: "bytedance"
```

注册 CRAWLERS "official"。

---

## Task 3.4 — 微信公众号爬虫

创建 **`crawlers/sources/wechat_crawler.py`**:
- 继承 BaseCrawler，source_name = "wechat"
- 两种实现方案，任选一种并在代码注释说明：
  - 方案 A（推荐）: 接入第三方 API 接口，从 config/sources.yaml 读取 wechat.accounts 列表
  - 方案 B（免费）: 通过搜狗微信搜索 weixin.sogou.com 采集公众号文章
- 从招聘相关公众号文章中提取岗位（正则匹配常见模式：岗位、城市、邮箱等）
 - 生成 Mock 版本并注释说明真实接口替换方式；Mock 默认关闭且不计入真实渠道验收

追加 config/sources.yaml wechat 配置：
```yaml
wechat:
  enabled: true
  schedule: "0 */3 * * *"
  accounts:
    - "名企校招招聘"
    - "应届生求职网"
```

注册 CRAWLERS "wechat"。

---

## Task 3.5 — Boss 直聘爬虫（反爬处理）

创建 **`crawlers/sources/boss_crawler.py`**:
- 继承 BaseCrawler，source_name = "boss"
- 使用 Playwright 而不是 httpx；遵守目标站点条款，不绕过 CAPTCHA：
  ```
  1. 启动 Playwright Chromium，headless=True（如遇到验证码则 False）
  2. 加载已保存的 storage_state（从 data/boss_context/state.json 读取）
  3. 访问 https://www.zhipin.com/web/geek/job?query=... 搜索页
  4. 检查是否出现 CAPTCHA 元素，有则记录日志并暂停（返回现有结果）
  5. 解析搜索结果列表（class="job-card-wrapper" 等）
  6. 翻页 2-3 页，每次间隔 10s
  7. 保存 storage_state 到 data/boss_context/state.json
  ```
- parse_item() 提取岗位信息
- 低频采集：间隔 sleep_random(10, 20) 防止触发反爬

更新 requirements.txt 追加 playwright；并注释需运行 `python -m playwright install chromium`。

注册 CRAWLERS "boss"。

---

## Task 3.6 — 智联招聘爬虫

创建 **`crawlers/sources/zhaopin_crawler.py`**:
- 继承 BaseCrawler，source_name = "zhaopin"
- 分析智联前端 API 接口模式；接口不可验证时标记为 degraded，不以 Mock 伪装为健康：
  ```
  假设接口: https://fe-api.zhaopin.com/c/i/sou
  Query: pageSize, pageNumber, cityId, keyword
  ```
- 用 httpx 调用，注意 UA 和 Referer
- token 过期处理：捕获 401/未授权响应，记录日志

注册 CRAWLERS "zhaopin"。

---

## Task 3.7 — 调度器扩展与失败告警

更新 **`crawlers/scheduler.py`**:
- setup_scheduler() 读取全部 8 个渠道注册 cron
- 每个渠道失败时，如果连续失败 3 次 → 调用 PushManager（如果已可用）发送告警
- 告警必须按渠道和失败次数幂等，不能每次调度都重复发送
- 添加函数 `get_scheduler_status()`：返回每个渠道的状态（healthy/degraded/paused）
- 在 crawlers/__init__.py 暴露 `get_crawler_list()` 工具函数返回所有已注册的渠道名

更新 **`config/sources.yaml`**，补全 8 个渠道的 schedule：
```yaml
sources:
  sasac:    {enabled: true, schedule: "0 */6 * * *"}
  mohrss:   {enabled: true, schedule: "0 */6 * * *"}
  official: {enabled: true, schedule: "0 */4 * * *"}
  wechat:   {enabled: true, schedule: "0 */3 * * *"}
  iguopin:  {enabled: true, schedule: "*/30 * * * *"}
  boss:     {enabled: true, schedule: "*/20 * * * *"}
  job51:    {enabled: true, schedule: "*/30 * * * *"}
  zhaopin:  {enabled: true, schedule: "*/30 * * * *"}
```

---

## Task 3.8 — 前端：完善岗位详情页

完善 **`web/src/views/JobDetail.vue`**（如果阶段一只有简单实现则重写）：

组件结构：
1. 返回按钮 + 面包屑（岗位列表 / 详情）
2. 基本信息卡片：
   - 标题 + 匹配星级（如果登录且有匹配记录）
   - 公司名 + 标签（上市/大型/...）
   - 城市 · 学历 · 届数 · 薪资 图标展示
3. 公司信息区域：行业 / 企业类型 / 发布日期 / 来源渠道
4. 技能标签云（skills_extracted JSON 渲染为 el-tag 列表）
5. 匹配评分区域（条件渲染：仅当有匹配记录时显示）：
   - 总分大星级显示
   - 5 个维度条形图（学历/技能/经验/地域/专业 0-5 分用 el-progress）
   - 匹配原因文字说明
   - highlights 亮点列表
6. JD 全文区域（pre 或 Markdown 渲染，支持换行缩进）
7. 操作区：
   - 「查看原始岗位」跳转 source_url（新窗口）
   - 「标记已投递」按钮
   - 「不感兴趣」按钮

---

## Task 3.9 — 前端：采集看板 Dashboard

创建 **`web/src/views/Dashboard.vue`**：

1. **顶部 4 个统计卡片**（el-row + el-col）：
   - 全年收录岗位总数（大字，数字跳动动画）
   - 今日新增岗位数
   - 今日可投岗位数
   - 最后更新时间（每 30s 轮询刷新）

2. **采集状态表格**（el-table）：
   列：渠道名 | 状态标签（success绿色 / degraded橙色 / failed红色） | 上次采集时间 | 新增数 | 耗时(s) | 操作（「立即采集」按钮）
   - 立即采集按钮调用 triggerCrawl(source)

3. **最近采集日志时间线**（el-timeline）：
   取最近 20 条 CrawlLog，状态图标区分成功/失败，失败可展开看 error_msg

4. **来源分布饼图**（echarts pie）：
   调用 stats 接口的 sources 数据，显示各渠道占比

5. **7 日新增趋势折线图**（echarts line）：
   新增接口 `GET /api/jobs/daily-trend?days=7` 返回过去 7 天每日新增数数组

创建 **`web/src/api/dashboard.ts`**：
- getCrawlStatus(): 返回各渠道上次 CrawlLog + 状态
- getCrawlLogs(params): CrawlLog 分页
- getDailyTrend(days=7): 每日新增数组
- getSourceDistribution(): 各源占比

对应后端新增接口：
- **GET /api/crawl/status** → 返回所有渠道状态（遍历 CRAWLERS 名 + 查最近 CrawlLog）
- **GET /api/jobs/daily-trend** → 按 publish_date 或 created_at 分组统计每日新增

---

## Task 3.10 — 前端：通知中心

创建 **`web/src/views/Notifications.vue`**：
- 通知列表（el-table，分页）
- 列：时间 | 渠道图标（Email/微信/钉钉/站内） | 岗位标题 | 匹配分 | 状态（sent 绿/failed 红）| 操作（查看）
- 点击行跳转到 /jobs/:job_id
- 「全部标记已读」按钮（本地存储管理已读状态）
- 筛选器：按渠道、按时间范围

---

## Task 3.11 — 完整路由与导航布局

更新 **`web/src/router/index.ts`**：
```typescript
const routes = [
  { path: '/', redirect: '/jobs' },
  { path: '/jobs', component: JobList, meta: { title: '岗位列表' } },
  { path: '/jobs/:id', component: JobDetail, meta: { title: '岗位详情' } },
  { path: '/matches', component: MatchResult, meta: { title: '匹配结果' } },
  { path: '/resume', component: ResumeUpload, meta: { title: '简历管理' } },
  { path: '/settings', component: Settings, meta: { title: '订阅设置' } },
  { path: '/dashboard', component: Dashboard, meta: { title: '采集看板' } },
  { path: '/notifications', component: Notifications, meta: { title: '通知中心' } },
]
```

更新 **`web/src/App.vue`**：
- 顶部导航栏：Logo "JobRadar" + 菜单 7 项（岗位列表/匹配结果/简历管理/采集看板/订阅设置/通知中心）
- 通知铃铛图标（el-badge 未读红点）
- 主题切换（可选）
- WebSocket 连接状态指示器（🔴/🟢）
- 底部版本号信息

创建 **`web/src/components/NotificationToast.vue`**：
- WebSocket 消息到达时右上角滑入通知
- 显示：岗位标题 + 匹配分星级 + 「查看」按钮
- 10 秒自动消失或手动关闭
- App.vue 全局挂载该组件

---

## 验收标准

1. crawlers/__init__.py CRAWLERS 字典中 8 个渠道全部注册
2. crawlers/sources/ 目录下有 8 个对应文件
3. config/sources.yaml 中 8 渠道都有 schedule 配置
4. 前端 7 个页面路由全部可访问（点击导航菜单）
5. Dashboard 展示 4 个统计卡片 + 状态表 + 饼图 + 折线图
6. JobDetail 展示匹配维度条形图（如存在匹配记录）
7. NotificationToast 组件接入 WebSocket 实时通知
8. 后端新增 GET /api/crawl/status 和 GET /api/jobs/daily-trend 接口
9. Mock 数据默认关闭；未配置真实接口的渠道状态为 `degraded`，不能伪装成健康采集
10. Boss 遇到 CAPTCHA 时记录并暂停，不自动绕过验证；所有异步爬虫不阻塞事件循环
