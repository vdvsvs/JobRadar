# ⭐ 快速开始 — Codex 客户端版

---

## 🎯 开始之前的准备工作

### 1. 在 Codex 客户端中打开项目文件夹

1. 打开 **Codex 客户端**
2. 点击菜单 **File → Open Folder**（文件 → 打开文件夹）
3. 浏览选择当前文件夹 `job-radar/`
4. 确认左侧文件树能看到：
   - `AGENTS.md` ✅
   - `codex-使用指南.md`
   - 本文件 `⭐0-快速开始.md`
   - `prompts/` 子目录

> 💡 Codex 客户端会**自动读取**当前目录下的 `AGENTS.md`，里面包含了项目的技术栈、目录结构、编码规范等所有上下文。**你不需要手动复制 AGENTS.md 的内容！**

---

## 🚀 执行顺序（严格按顺序）

### Step 1: 执行阶段一（MVP 核心闭环）

✅ **完成后你将得到**：项目骨架 + 数据库 + 2个爬虫 + 数据处理 + 基础API + 前端岗位列表 + Docker

**操作步骤**：
1. 在 Codex 客户端左侧文件树中，**双击打开** `prompts/1-阶段一-MVP.md`
2. 按 `Ctrl + A` 全选文件内容
3. 按 `Ctrl + C` 复制
4. 点击客户端底部的对话框输入框
5. 按 `Ctrl + V` 粘贴
6. 按 **Enter** 或点击「发送」按钮
7. ⏳ 等待生成完成（通常 1-5 分钟，如果内容太长分多次也没关系）

---

### Step 2: 生成完成后运行验证

在你的 Windows PowerShell / CMD / 终端中执行：

```powershell
cd <你存放项目的路径>\job-radar

# 安装依赖
pip install -r requirements.txt

# 复制环境变量模板
copy .env.example .env

# 初始化数据库
python -c "from database.seed import init_db; init_db()"

# 启动 API 服务
uvicorn api.main:app --reload --port 8000
```

打开另一个终端测试：
```powershell
curl http://localhost:8000/api/jobs/stats
```

应该返回类似：
```json
{"total_jobs": 0, "today_new": 0, "sources": {}}
```

---

### Step 3: 对照「阶段一验收标准」逐项检查

打开 `prompts/1-阶段一-MVP.md` 文件底部的「验收标准」部分，逐条确认：
- [ ] 所有列出的文件都已创建
- [ ] `pip install -r requirements.txt` 无报错
- [ ] 数据库初始化成功，`data/jobradar.db` 文件存在
- [ ] API 服务启动正常，返回 JSON
- [ ] `trigger/job51` 接口返回 triggered
- [ ] 默认关闭 Mock；未接通真实接口时渠道显示 degraded，不能以模拟岗位通过验收
- [ ] 前端 `pnpm install && pnpm dev` 能启动，浏览器可见岗位列表
- [ ] `docker compose config` 解析配置无报错

**⚠️ 所有项都打勾后，再进入阶段二。不要跳步骤！**

---

### Step 4: 按相同模式依次执行 阶段二 → 三 → 四

| 阶段 | 文件 | 核心功能 |
|------|------|---------|
| **阶段二** | `prompts/2-阶段二-AI匹配与推送.md` | 简历上传 + AI画像提取 + 向量匹配 + LLM评分 + 推送系统 + WebSocket |
| **阶段三** | `prompts/3-阶段三-全渠道与前端.md` | 剩余6个渠道爬虫 + 采集看板 + 通知中心 + 完整前端页面 |
| **阶段四** | `prompts/4-阶段四-稳定性与运维.md` | 健康检查 + 自动降级 + 日志轮转 + 数据备份 + 性能优化 + README |

每个阶段完成后，打开文件底部的「验收标准」部分逐项打勾。

---

## 🆘 遇到问题怎么办？

### Q1: Codex 客户端生成的代码被截断了？
在对话框中直接输入：
```
请继续刚才的生成，从被截断的位置继续。
```

### Q2: 生成的代码运行报错？
把**完整的错误信息**（红色文字全部）贴到对话框：
```
运行时报错：
--- 粘贴错误信息 ---
请修复。
```

### Q3: 爬虫抓不到真实数据？
爬虫提示词中已说明：无法直接抓取时先生成 Mock 数据版本。后续你自己用浏览器抓包分析了真实接口后，再追加指令修正：
```
iguopin_crawler.py 目前是 Mock 版本。
我已经抓包分析国聘网的真实搜索接口：
  POST https://www.iguopin.com/api/job/search
  请求体: {page: 1, pageSize: 20}
请根据这个真实接口重写爬虫。
```

### Q4: 某个 Task 想单独重新生成？
```
Task 1.6 中 api/routers/jobs.py 的筛选逻辑有问题。
请不要修改其他文件，只重写 api/routers/jobs.py：
要求支持 keyword 模糊搜索 title/company/jd_text 三个字段，
支持 city/education/source 精确匹配，支持分页。
```

### Q5: 生成内容太多，想分批？
直接在阶段文件内容**开头**追加一句：
```
请先只完成 Task 1.1 ~ Task 1.4，剩余 Task 等我下一轮再要求生成。
```

---

## 📖 更多信息

- 详细使用说明 → 打开 [codex-使用指南.md](codex-使用指南.md)
- 完整设计文档 → 打开 [全网岗位监控系统-任务书.md](全网岗位监控系统-任务书.md)
