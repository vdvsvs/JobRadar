# 全网岗位监控系统 — 项目任务书

> **项目代号**：JobRadar
> **文档版本**：v1.0
> **创建日期**：2026-08-14
> **项目性质**：个人使用的岗位聚合监控工具

---

## 一、项目概述

### 1.1 项目目标

构建一个个人使用的自动化岗位监控系统，实现：

- 从 **8 大招聘渠道** 定时采集新增岗位数据
- 对原始数据进行 **清洗、去重、结构化**
- 基于 AI 计算用户简历与岗位 JD 的 **匹配度评分**
- 对高匹配新岗位进行 **实时推送通知**

### 1.2 核心价值

| 痛点 | 解决方案 |
|------|----------|
| 需要每天手动浏览多个招聘网站 | 统一聚合，一个面板看全网 |
| 容易错过新发布的好岗位 | 分钟级监控，新岗位即时推送 |
| 岗位描述格式不统一 | AI 结构化解析 + 匹配评分 |
| 无法快速判断岗位是否适合自己 | AI 人岗匹配度评分（1-5 星） |

### 1.3 监控渠道清单

| # | 渠道名称 | 类型 | 网址 | 采集方式 |
|---|---------|------|------|---------|
| 1 | 国务院国资委 | 官方 | http://www.sasac.gov.cn | HTML 爬虫 |
| 2 | 中国人社部 | 官方 | http://www.mohrss.gov.cn | HTML 爬虫 / RSS |
| 3 | 企业官网 | 官方 | 多站点 | 定向爬虫（按企业列表） |
| 4 | 微信公众号 | 官方 | 多账号 | 公众号文章采集 / 第三方 API |
| 5 | 国聘网 | 商业 | https://www.iguopin.com | HTML 爬虫 + API 逆向 |
| 6 | Boss 直聘 | 商业 | https://www.zhipin.com | API 逆向 + 反爬处理 |
| 7 | 前程无忧 | 商业 | https://www.51job.com | HTML 爬虫 |
| 8 | 智联招聘 | 商业 | https://www.zhaopin.com | HTML 爬虫 |

> **可选扩展渠道**：猎聘网 (liepin.com)、牛客网 (nowcoder.com)、拉勾网 (lagou.com)

---

## 二、系统架构设计

### 2.1 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户交互层 (Frontend)                   │
│         Web Dashboard (Vue3 + Element Plus)              │
│   简历上传 / 岗位浏览 / 匹配筛选 / 订阅设置 / 通知查看       │
└────────────────────────┬────────────────────────────────┘
                         │ REST API + WebSocket
┌────────────────────────┴────────────────────────────────┐
│                    API 服务层 (Backend)                   │
│                    FastAPI (Python)                      │
│   岗位查询 / 简历管理 / 匹配触发 / 订阅管理 / 推送调度       │
└────────────────────────┬────────────────────────────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
┌───────┴───────┐ ┌──────┴───────┐ ┌──────┴───────┐
│   采集引擎层    │ │  数据处理层   │ │   AI 匹配层   │
│  (Crawlers)    │ │ (Pipeline)  │ │  (Matching)  │
│                │ │              │ │              │
│ 8 个渠道爬虫   │ │ 清洗/去重/   │ │ 简历画像提取  │
│ 定时调度器     │ │ 结构化/打标签│ │ JD 语义解析   │
│ 增量检测       │ │ 入库/索引    │ │ 匹配度评分    │
└───────────────┘ └──────────────┘ └──────────────┘
        │                │                │
        └────────────────┼────────────────┘
                         │
                ┌────────┴────────┐
                │    存储层 (DB)    │
                │  SQLite / MySQL  │
                │  岗位/简历/订阅/  │
                │  匹配记录/日志    │
                └─────────────────┘
                         │
                ┌────────┴────────┐
                │   推送层 (Push)  │
                │ 邮件/微信/钉钉/  │
                │ 站内 WebSocket  │
                └─────────────────┘
```

### 2.2 技术选型

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| **爬虫引擎** | Python + Scrapy / Playwright | Scrapy 适合大规模并发；Playwright 处理 JS 渲染页面 |
| **反爬处理** | httpx + fake-useragent + 代理池 | httpx 支持 HTTP/2；代理池轮换 IP |
| **API 服务** | FastAPI + Uvicorn | 异步高性能，自动生成 API 文档 |
| **定时调度** | APScheduler / Celery Beat | APScheduler 轻量适合个人项目；Celery 适合扩展 |
| **数据库** | SQLite（初期）→ MySQL（扩展） | 个人项目 SQLite 零配置；数据量大后迁移 MySQL |
| **AI 匹配** | OpenAI API / 本地 Embedding 模型 | 向量相似度 + LLM 评分双引擎 |
| **向量存储** | ChromaDB / FAISS | 轻量级向量数据库，支持本地部署 |
| **前端** | Vue3 + Element Plus + AG Grid | Element Plus 组件丰富；AG Grid 处理大量岗位表格 |
| **推送** | smtplib(邮件) + Server酱(微信) | 零成本推送方案 |
| **部署** | Docker Compose | 一键启动所有服务 |

### 2.3 项目目录结构

```
job-radar/
├── docker-compose.yml
├── .dockerignore
├── .gitignore
├── .env.example
├── README.md
│
├── crawlers/                    # 采集引擎层
│   ├── base/
│   │   ├── __init__.py
│   │   ├── base_crawler.py      # 爬虫基类
│   │   ├── anti_detect.py       # 反爬工具（UA轮换/代理/限速）
│   │   └── parser_base.py       # 解析器基类
│   ├── sources/
│   │   ├── sasac_crawler.py     # 国资委
│   │   ├── mohrss_crawler.py    # 人社部
│   │   ├── official_crawler.py  # 企业官网
│   │   ├── wechat_crawler.py    # 微信公众号
│   │   ├── iguopin_crawler.py   # 国聘网
│   │   ├── boss_crawler.py      # Boss直聘
│   │   ├── job51_crawler.py     # 前程无忧
│   │   └── zhaopin_crawler.py   # 智联招聘
│   ├── scheduler.py             # 定时调度器
│   └── incremental.py          # 增量检测器
│
├── pipeline/                    # 数据处理层
│   ├── cleaner.py               # 数据清洗（去HTML/去噪/格式归一）
│   ├── deduplicator.py          # 去重（标题+公司+MD5指纹）
│   ├── normalizer.py           # 字段标准化（学历/城市/薪资/专业）
│   ├── tagger.py                # 打标签（行业/企业类型/技能词）
│   └── storage.py               # 入库 + 索引
│
├── matching/                    # AI 匹配层
│   ├── resume_parser.py         # 简历解析 → 用户画像
│   ├── jd_parser.py             # JD 解析 → 岗位画像
│   ├── embedding_engine.py      # 向量化引擎
│   ├── similarity_scorer.py    # 向量相似度粗筛
│   ├── llm_scorer.py            # LLM 精细评分
│   └── match_engine.py          # 匹配调度入口
│
├── api/                         # API 服务层
│   ├── main.py                  # FastAPI 入口
│   ├── routers/
│   │   ├── jobs.py              # 岗位查询接口
│   │   ├── resume.py            # 简历管理接口
│   │   ├── matching.py          # 匹配触发接口
│   │   ├── subscription.py     # 订阅管理接口
│   │   └── notification.py     # 通知接口
│   └── websocket.py             # WebSocket 实时推送
│
├── push/                        # 推送层
│   ├── email_pusher.py          # 邮件推送
│   ├── wechat_pusher.py         # 微信推送（Server酱）
│   ├── dingtalk_pusher.py       # 钉钉推送
│   └── push_manager.py          # 推送调度管理
│
├── web/                         # 前端
│   ├── src/
│   │   ├── views/
│   │   │   ├── JobList.vue       # 岗位列表
│   │   │   ├── ResumeUpload.vue # 简历上传
│   │   │   ├── MatchResult.vue  # 匹配结果
│   │   │   └── Settings.vue     # 订阅设置
│   │   └── ...
│   └── ...
│
├── database/                    # 数据库
│   ├── models.py                # ORM 模型
│   ├── migrations/              # 数据库迁移（schema/index 变更必须执行）
│   └── seed.py                  # 初始化数据
│
├── config/
│   ├── settings.py              # 全局配置
│   ├── sources.yaml             # 渠道配置
│   └── matching_rules.yaml      # 匹配规则配置
│
└── tests/                       # 测试
    ├── test_crawlers/
    ├── test_pipeline/
    └── test_matching/
```

---

## 三、数据库设计

### 3.1 核心表结构

#### `jobs` — 岗位表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| source | VARCHAR(32) | 来源渠道（sasac/boss/51job...） |
| source_url | TEXT | 原始链接 |
| source_job_id | VARCHAR(128) | 来源站点的岗位 ID |
| title | VARCHAR(256) | 岗位标题 |
| company | VARCHAR(128) | 公司名称 |
| company_type | VARCHAR(32) | 企业类型（上市/大型/中型/小型） |
| industry | VARCHAR(64) | 行业 |
| city | VARCHAR(64) | 城市 |
| education | VARCHAR(32) | 学历要求 |
| graduation_year | VARCHAR(16) | 届数要求（如 26届/27届） |
| major_required | TEXT | 专业要求（逗号分隔） |
| salary | VARCHAR(64) | 薪资范围 |
| jd_text | TEXT | 岗位描述原文 |
| jd_clean | TEXT | 清洗后的 JD |
| skills_extracted | JSON | AI 提取的技能标签 |
| job_hash | VARCHAR(64) | 去重指纹（规范化 title + company + city，字段用 NUL 分隔后 MD5） |
| publish_date | DATE | 发布日期 |
| crawl_time | DATETIME | 采集时间 |
| is_active | BOOLEAN | 是否仍可投递 |
| created_at | DATETIME | 入库时间 |

#### `resumes` — 简历表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| file_name | VARCHAR(256) | 原始文件名 |
| file_path | TEXT | 存储路径 |
| raw_text | TEXT | 提取的纯文本 |
| profile | JSON | AI 解析的用户画像 |
| embedding | BLOB | 简历向量 |
| is_default | BOOLEAN | 是否默认简历 |
| created_at | DATETIME | 上传时间 |

> `profile` JSON 示例：
> ```json
> {
>   "education": "硕士",
>   "school": "XXX大学",
>   "major": "计算机科学与技术",
>   "graduation_year": "2027",
>   "skills": ["Python", "FastAPI", "Vue", "MySQL"],
>   "experience_years": 2,
>   "preferred_cities": ["北京", "上海", "深圳"],
>   "preferred_industries": ["互联网", "金融科技"],
>   "preferred_roles": ["后端开发", "全栈开发"]
> }
> ```

#### `matches` — 匹配记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| resume_id | INTEGER FK | 简历 ID |
| job_id | INTEGER FK | 岗位 ID |
| score | FLOAT | 匹配度评分（0.0-5.0） |
| score_detail | JSON | 各维度评分明细 |
| match_reason | TEXT | AI 生成的原因说明 |
| is_notified | BOOLEAN | 是否已推送通知 |
| created_at | DATETIME | 匹配时间 |

#### `subscriptions` — 订阅规则表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | VARCHAR(64) | 订阅名称 |
| resume_id | INTEGER FK | 关联简历 |
| keywords | TEXT | 关键词（逗号分隔） |
| cities | TEXT | 订阅城市 |
| min_score | FLOAT | 最低匹配分阈值（默认 3.0） |
| channels | JSON | 推送渠道配置 |
| is_active | BOOLEAN | 是否启用 |
| created_at | DATETIME | 创建时间 |

#### `crawl_logs` — 采集日志表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| source | VARCHAR(32) | 渠道 |
| status | VARCHAR(16) | success / failed |
| new_count | INTEGER | 新增岗位数 |
| error_msg | TEXT | 错误信息 |
| duration_sec | FLOAT | 耗时（秒） |
| created_at | DATETIME | 记录时间 |

---

## 四、模块详细任务分解

### 模块 A：采集引擎层

#### A1 — 爬虫基类与基础设施

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| A1.1 | 实现 `BaseCrawler` 基类 | 支持统一接口：`fetch()` → `parse()` → `normalize()`；子类只需实现具体解析逻辑 |
| A1.2 | 实现反爬工具模块 | UA 随机轮换、请求间隔随机延迟（2-5s）、自动重试（最多 3 次）、超时控制（30s） |
| A1.3 | 实现代理池管理 | 支持配置代理列表；自动轮换；失败自动切换；可选接入免费/付费代理 |
| A1.4 | 实现增量检测器 | 基于岗位 hash 去重；只处理新增/变更岗位；记录上次采集位点（分页/时间戳） |
| A1.5 | 实现采集调度器 | 基于 APScheduler；支持每个渠道独立配置 cron 表达式；支持手动触发 |

**调度配置建议**：

```yaml
# config/sources.yaml
sources:
  sasac:
    enabled: true
    schedule: "0 */6 * * *"      # 每6小时
    url: "http://www.sasac.gov.cn"
  mohrss:
    enabled: true
    schedule: "0 */6 * * *"
    url: "http://www.mohrss.gov.cn"
  iguopin:
    enabled: true
    schedule: "*/30 * * * *"     # 每30分钟
    url: "https://www.iguopin.com"
  boss:
    enabled: true
    schedule: "*/20 * * * *"     # 每20分钟
    url: "https://www.zhipin.com"
  job51:
    enabled: true
    schedule: "*/30 * * * *"
    url: "https://www.51job.com"
  zhaopin:
    enabled: true
    schedule: "*/30 * * * *"
    url: "https://www.zhaopin.com"
  official:
    enabled: true
    schedule: "0 */4 * * *"      # 每4小时
    url: ""                       # 企业列表动态配置
  wechat:
    enabled: true
    schedule: "0 */3 * * *"      # 每3小时
    accounts: []                  # 公众号列表
```

#### A2 — 各渠道爬虫实现

| 任务 ID | 渠道 | 实现要点 | 难度 |
|---------|------|---------|------|
| A2.1 | 国资委 | 解析招聘公告列表页；提取公告详情中的岗位信息；无反爬 | ★☆☆ |
| A2.2 | 人社部 | 解析就业招聘频道；RSS 或 HTML 采集；提取岗位字段 | ★☆☆ |
| A2.3 | 企业官网 | 维护企业招聘 URL 列表；通用解析模板 + 特殊站适配器；处理各站不同结构 | ★★☆ |
| A2.4 | 微信公众号 | 接入第三方 API（如 WeSearch）；或通过搜狗微信搜索采集；注意频率限制 | ★★☆ |
| A2.5 | 国聘网 | 分析 API 接口；携带 cookie/token；处理分页 | ★★☆ |
| A2.6 | Boss 直聘 | API 逆向分析；反爬较严（指纹检测/CAPTCHA）；需 Playwright 辅助；建议低频采集 | ★★★ |
| A2.7 | 前程无忧 | HTML 列表页解析；POST 搜索接口；注意编码处理（GBK） | ★★☆ |
| A2.8 | 智联招聘 | API 接口分析；处理 token 刷新；分页采集 | ★★☆ |

**每个爬虫的统一输出格式**：

```python
@dataclass
class RawJobItem:
    source: str           # 来源标识
    source_url: str       # 原始链接
    source_job_id: str    # 来源岗位ID
    title: str            # 岗位标题
    company: str          # 公司名称
    city: str             # 城市
    education: str        # 学历要求
    salary: str           # 薪资范围
    jd_text: str          # 岗位描述原文
    publish_date: str     # 发布日期
    raw_data: dict        # 原始完整数据（备用）
```

#### A3 — 采集容错与监控

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| A3.1 | 实现采集失败告警 | 连续 3 次失败 → 推送通知告知用户某渠道异常 |
| A3.2 | 实现采集日志看板 | 记录每次采集的时间/新增数/失败数/耗时；前端可查看 |
| A3.3 | 实现自动降级 | 某渠道反爬升级时自动降低频率；不影响其他渠道 |

---

### 模块 B：数据处理层

#### B1 — 数据清洗

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| B1.1 | HTML 标签清洗 | 去除 JD 中的 HTML 标签、脚本、样式；保留纯文本 |
| B1.2 | 文本去噪 | 去除多余空白、特殊字符、广告水印文字 |
| B1.3 | 编码统一处理 | 处理 GBK/UTF-8 混合编码；统一输出 UTF-8 |

#### B2 — 去重

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| B2.1 | 精确去重 | 规范化后的 `MD5(title + "\\0" + company + "\\0" + city)` 作为指纹；完全相同则丢弃 |
| B2.2 | 模糊去重 | 同公司同岗位标题相似度 >90% 视为重复（使用 Levenshtein 距离或 SimHash） |
| B2.3 | 跨渠道去重 | 当前 `jobs` 表只有单个 source/source_url，暂不执行跨渠道合并；确认增加来源关联表后再实现并保留所有来源链接 |

#### B3 — 字段标准化

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| B3.1 | 学历归一 | "本科及以上"→"本科"；"硕士优先"→"硕士优先"；"大专以上"→"大专" |
| B3.2 | 城市归一 | "北京市"→"北京"；"上海市"→"上海"；支持多城市拆分 |
| B3.3 | 薪资归一 | "15k-25k"→{min:15000, max:25000, unit:"月"}；"面议"→null |
| B3.4 | 专业归一 | "计算机科学与技术"→"计算机"；"电子信息工程"→"电子信息" |
| B3.5 | 届数归一 | "2027届"→"27届"；"应届生"→当前年届 |
| B3.6 | 企业类型分类 | 基于工商信息/注册资本判断：上市/大型/中型/小型/创业 |

#### B4 — 打标签

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| B4.1 | 技能标签提取 | 基于 JD 文本用关键词词典匹配：Python/Java/Vue/MySQL/K8s 等 |
| B4.2 | 行业标签 | 基于公司名 + JD 内容判断行业：互联网/金融/制造/教育等 |
| B4.3 | 岗位类型标签 | 后端/前端/算法/产品/运营/设计/测试/运维 |

---

### 模块 C：AI 匹配引擎

#### C1 — 简历解析

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| C1.1 | 简历文件解析 | 支持 PDF/DOCX 格式；使用 pdfplumber / python-docx 提取文本 |
| C1.2 | LLM 结构化解析 | 调用 LLM 将简历文本解析为结构化 JSON 画像（学历/专业/技能/经验/偏好） |
| C1.3 | 简历向量化 | 将简历画像转为 embedding 向量存入 ChromaDB |

**LLM 解析 Prompt 示例**：

```
你是一个简历解析专家。请从以下简历文本中提取结构化信息，输出 JSON 格式。

简历文本：
{resume_text}

请提取以下字段（如果不存在则为 null）：
- name: 姓名
- education: 最高学历（大专/本科/硕士/博士）
- school: 毕业院校
- major: 专业
- graduation_year: 毕业年份（格式：26届/27届）
- skills: 技能列表（数组）
- experience_years: 工作经验年限
- preferred_cities: 期望城市（数组）
- preferred_roles: 期望岗位类型（数组）
- preferred_industries: 期望行业（数组）
- self_summary: 一句话自我总结

只输出 JSON，不要其他文字。
```

#### C2 — JD 解析

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| C2.1 | JD 技能提取 | 从 JD 文本中提取硬性技能要求和加分技能 |
| C2.2 | JD 结构化 | 提取：岗位职责/任职要求/工作地点/薪资范围 |
| C2.3 | JD 向量化 | 将 JD 文本转为 embedding 向量 |

#### C3 — 匹配引擎

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| C3.1 | 向量相似度粗筛 | 简历 embedding vs JD embedding 计算余弦相似度；快速筛选 Top N 候选岗位 |
| C3.2 | LLM 精细评分 | 将简历画像 + JD 结构化数据送入 LLM；输出 0-5 分匹配度 + 评分理由 |
| C3.3 | 硬性条件过滤 | 学历/届数/城市/专业 等硬性条件不满足的直接排除 |
| C3.4 | 增量匹配触发 | 每次新增岗位入库后 → 自动触发匹配 → 命中则推送 |
| C3.5 | 匹配结果缓存 | 已匹配过的岗位不重复评分；简历更新后清除旧缓存重算 |

**LLM 评分 Prompt 示例**：

```
你是一个专业的人岗匹配评估专家。

候选人画像：
{resume_profile_json}

岗位信息：
{job_detail_json}

请评估该候选人与岗位的匹配度，输出 JSON：
{
  "score": 0.0-5.0,        // 总分
  "dimensions": {
    "education_match": 0-5,   // 学历匹配度
    "skill_match": 0-5,      // 技能匹配度
    "experience_match": 0-5,  // 经验匹配度
    "location_match": 0-5,   // 地域匹配度
    "major_match": 0-5       // 专业匹配度
  },
  "reason": "一句话说明匹配/不匹配原因",
  "highlights": ["匹配亮点1", "匹配亮点2"]
}

只输出 JSON，不要其他文字。
```

#### C4 — 向量引擎配置

| 任务 ID | 任务描述 | 验收标准 |
|---------|---------|---------|
| C4.1 | 部署 ChromaDB | Docker 部署；创建 jobs_collection 和 resumes_collection |
| C4.2 | Embedding 模型选型 | 使用 OpenAI text-embedding-3-small 或本地 BGE 模型 |
| C4.3 | 向量索引维护 | 新岗位入库时同步写入向量；支持定期重建索引 |

---

### 模块 D：API 服务层

#### D1 — 核心 API 设计

| 任务 ID | 接口 | 方法 | 说明 |
|---------|------|------|------|
| D1.1 | `/api/jobs` | GET | 岗位列表查询，支持分页/筛选/排序 |
| D1.2 | `/api/jobs/{id}` | GET | 岗位详情 |
| D1.3 | `/api/jobs/stats` | GET | 统计信息（总量/今日新增/来源分布） |
| D1.4 | `/api/resume/upload` | POST | 上传简历文件（PDF/DOCX） |
| D1.5 | `/api/resume/{id}` | GET | 获取简历画像 |
| D1.6 | `/api/match/trigger` | POST | 手动触发匹配 |
| D1.7 | `/api/match/results` | GET | 查询匹配结果列表 |
| D1.8 | `/api/subscription` | CRUD | 订阅规则增删改查 |
| D1.9 | `/api/notifications` | GET | 通知记录列表 |
| D1.10 | `/api/crawl/trigger` | POST | 手动触发某渠道采集 |
| D1.11 | `/api/crawl/logs` | GET | 采集日志查看 |
| D1.12 | `/ws/notifications` | WS | WebSocket 实时推送通知 |

#### D2 — 筛选查询设计

岗位列表支持以下筛选维度：

```
GET /api/jobs?
  keyword=Python              # 关键词搜索（标题+JD全文）
  city=北京,上海               # 城市筛选（多选）
  education=本科              # 学历筛选
  company_type=上市,大型      # 企业类型
  industry=互联网             # 行业筛选
  graduation_year=27届        # 届数筛选
  min_salary=15000            # 最低月薪
  source=boss,zhaopin         # 来源渠道
  match_score=3.0             # 最低匹配分（需登录）
  sort_by=publish_date        # 排序字段
  sort_order=desc             # 排序方向
  page=1&page_size=50         # 分页
```

---

### 模块 E：推送系统

#### E1 — 推送渠道实现

| 任务 ID | 渠道 | 实现方式 | 说明 |
|---------|------|---------|------|
| E1.1 | 邮件 | smtplib + SMTP | 支持 HTML 邮件模板；汇总推送（每日/实时） |
| E1.2 | 微信 | Server酱 / 企业微信机器人 | 免费方案用 Server酱；企业微信机器人无限制 |
| E1.3 | 钉钉 | 钉钉自定义机器人 Webhook | 支持 Markdown 消息卡片 |
| E1.4 | 站内 | WebSocket | 前端实时弹窗通知 |

#### E2 — 推送策略

| 任务 ID | 策略 | 说明 |
|---------|------|------|
| E2.1 | 实时推送 | 新岗位匹配分 ≥4.0 → 立即推送单条通知 |
| E2.2 | 汇总推送 | 匹配分 3.0-4.0 的岗位 → 每日定时汇总推送（如每天 9:00） |
| E2.3 | 防重复 | 已推送过的岗位不重复推送；记录 `is_notified` 状态 |
| E2.4 | 推送模板 | 每条通知包含：岗位名称 + 公司 + 匹配分 + 匹配原因 + 详情链接 |

**邮件推送模板**：

```
Subject: [JobRadar] 发现 3 个高匹配新岗位

📊 今日匹配摘要：
  - 新增高匹配岗位：3 个
  - 匹配评分 ≥4.0

━━━━━━━━━━━━━━━━━━━━━━━

⭐⭐⭐⭐⭐ 大模型应用算法工程师 | 小红书
   匹配度：4.8/5.0
   城市：北京 | 学历：本科 | 薪资：25k-40k
   匹配原因：你的 Python/PyTorch 技能高度匹配，
            有大模型微调经验加分
   👉 查看详情：http://localhost:8080/jobs/12345

⭐⭐⭐⭐☆ 后端开发工程师 | 哔哩哔哩
   匹配度：4.2/5.0
   城市：上海 | 学历：本科 | 薪资：20k-35k
   匹配原因：FastAPI 经验直接匹配，
            缺少 Go 经验扣分
   👉 查看详情：http://localhost:8080/jobs/12346

━━━━━━━━━━━━━━━━━━━━━━━
JobRadar · 你的 AI 求职助手
```

---

### 模块 F：前端 Web 界面

#### F1 — 页面规划

| 页面 | 路由 | 功能说明 |
|------|------|---------|
| 岗位列表 | `/jobs` | AG Grid 表格展示；支持多维度筛选；点击行展开详情 |
| 岗位详情 | `/jobs/:id` | JD 全文 + 匹配评分 + 评分维度雷达图 |
| 简历管理 | `/resume` | 上传简历；查看/编辑画像；切换默认简历 |
| 匹配结果 | `/matches` | 按匹配度排序的岗位列表；支持标记已投递/不感兴趣 |
| 订阅设置 | `/settings` | 配置关键词/城市/分数阈值/推送渠道 |
| 采集看板 | `/dashboard` | 各渠道采集状态/日志/统计图表 |
| 通知中心 | `/notifications` | 历史推送记录 |

#### F2 — 前端组件清单

| 组件 | 说明 |
|------|------|
| `JobTable` | AG Grid 表格，支持虚拟滚动（万级数据） |
| `FilterBar` | 筛选条件工具栏（城市/学历/行业/来源等） |
| `JobDetailDrawer` | 岗位详情抽屉（侧滑面板） |
| `MatchScore` | 匹配度评分组件（星级 + 维度条形图） |
| `ResumeUploader` | 简历上传组件（拖拽 + 进度） |
| `ResumeProfile` | 简历画像展示卡片 |
| `StatsCard` | 统计数据卡片（总量/今日新增/更新时间） |
| `CrawlStatus` | 采集状态指示器 |
| `NotificationToast` | WebSocket 实时通知弹窗 |

---

## 五、实施计划

### 5.1 阶段划分

#### 第一阶段：MVP 最小可用版（核心闭环）

> 目标：跑通「采集 → 入库 → 查询」基础流程

| 序号 | 任务 | 依赖 |
|------|------|------|
| 1 | 搭建项目骨架 + Docker Compose 环境 | 无 |
| 2 | 设计数据库表结构 + 创建 ORM 模型 | 1 |
| 3 | 实现 2 个最简爬虫（国聘网 + 前程无忧） | 2 |
| 4 | 实现数据清洗 + 去重 + 入库 Pipeline | 3 |
| 5 | 实现基础 API（岗位列表 + 详情） | 4 |
| 6 | 实现前端岗位列表页面（AG Grid） | 5 |
| 7 | 实现 APScheduler 定时调度 | 4 |

**MVP 验收**：系统定时从 2 个渠道采集岗位 → 清洗入库 → 前端表格展示 → 支持筛选查询

#### 第二阶段：AI 匹配 + 推送

> 目标：跑通「简历上传 → AI 匹配 → 推送通知」闭环

| 序号 | 任务 | 依赖 |
|------|------|------|
| 8 | 实现简历上传 + 文件解析 | 阶段一完成 |
| 9 | 实现 LLM 简历画像提取 | 8 |
| 10 | 部署 ChromaDB + Embedding 引擎 | 9 |
| 11 | 实现 JD 向量化 + 增量入库 | 10 |
| 12 | 实现向量相似度粗筛 + LLM 精细评分 | 11 |
| 13 | 实现匹配结果 API + 前端展示 | 12 |
| 14 | 实现邮件推送 + 微信推送 | 13 |
| 15 | 实现订阅规则配置 | 14 |

**阶段验收**：上传简历 → 系统自动匹配 → 匹配分 ≥4.0 的岗位推送到微信/邮箱

#### 第三阶段：全渠道覆盖 + 体验优化

> 目标：覆盖全部 8 大渠道 + 前端体验打磨

| 序号 | 任务 | 依赖 |
|------|------|------|
| 16 | 实现剩余 6 个渠道爬虫 | 阶段二完成 |
| 17 | 实现 Boss 直聘反爬处理（Playwright） | 16 |
| 18 | 实现企业官网 + 微信公众号采集 | 16 |
| 19 | 完善前端所有页面 | 阶段二完成 |
| 20 | 实现 WebSocket 实时推送 | 14 |
| 21 | 实现采集看板 + 日志查看 | 19 |
| 22 | 性能优化（索引/缓存/批量处理） | 全部 |

#### 第四阶段：稳定性 + 运维

| 序号 | 任务 |
|------|------|
| 23 | 添加健康检查 + 自动重启 |
| 24 | 实现数据备份策略 |
| 25 | 编写部署文档 |
| 26 | 压力测试 + 反爬策略调优 |

### 5.2 里程碑

| 里程碑 | 目标 | 交付物 |
|--------|------|--------|
| **M1** | MVP 可用 | 2 渠道采集 + 岗位列表查询 |
| **M2** | AI 匹配闭环 | 简历上传 + 匹配 + 推送 |
| **M3** | 全渠道覆盖 | 8 渠道采集 + 完整前端 |
| **M4** | 生产稳定 | Docker 部署 + 监控告警 |

---

## 六、配置文件模板

### 6.1 `.env.example`

```env
# ===== 数据库 =====
DATABASE_URL=sqlite:///data/jobradar.db
# MYSQL_URL=mysql://user:pass@localhost:3306/jobradar

# ===== LLM API =====
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://api.openai.com/v1
EMBEDDING_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini

# ===== 向量数据库 =====
CHROMA_HOST=localhost
CHROMA_PORT=8001

# ===== 推送配置 =====
# 邮件
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_USER=your_email@qq.com
SMTP_PASSWORD=your_auth_code
NOTIFY_EMAIL=your_email@qq.com

# Server酱（微信推送）
SERVERCHAN_KEY=SCT123456

# 钉钉机器人
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=xxx
DINGTALK_SECRET=SECxxx

# ===== 代理池（可选）=====
PROXY_POOL=http://localhost:5010/random
# 或直接配置代理列表
PROXY_LIST=http://ip1:port1,http://ip2:port2

# ===== 调度配置 =====
TIMEZONE=Asia/Shanghai
ALLOW_MOCK_DATA=false
```

### 6.2 `docker-compose.yml`

```yaml
version: '3.8'

services:
  # API 服务
  api:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file: .env
    environment:
      CHROMA_HOST: chromadb
      CHROMA_PORT: 8000
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    depends_on:
      - chromadb
    restart: unless-stopped

  # 向量数据库
  chromadb:
    image: chromadb/chroma:0.5.23
    ports:
      - "8001:8000"
    volumes:
      - ./data/chroma:/chroma/chroma
    restart: unless-stopped

  # 前端（生产构建）
  web:
    build: ./web
    ports:
      - "127.0.0.1:8080:80"
    depends_on:
      - api
    restart: unless-stopped

  # 调度器（独立进程）
  scheduler:
    build: .
    command: python -m crawlers.scheduler
    env_file: .env
    environment:
      CHROMA_HOST: chromadb
      CHROMA_PORT: 8000
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    depends_on:
      - api
    restart: unless-stopped
```

---

## 七、风险与注意事项

### 7.1 法律合规风险

| 风险 | 说明 | 应对措施 |
|------|------|---------|
| **爬虫法律风险** | 大规模爬取可能违反目标网站 ToS 或《数据安全法》 | ① 仅个人使用不对外提供服务；② 控制采集频率（≥20分钟间隔）；③ 不存储用户隐私数据；④ 优先使用官方 API/RSS |
| **数据版权** | 岗位数据版权归原平台所有 | ① 仅存储岗位摘要信息；② 不二次分发；③ 保留原始链接引导用户回到原平台 |
| **个人信息** | 爬取可能涉及招聘者个人信息 | 不采集 HR 个人联系方式/手机号等敏感信息 |

### 7.2 技术风险

| 风险 | 说明 | 应对措施 |
|------|------|---------|
| **反爬升级** | Boss 直聘等平台反爬能力强 | ① 使用 Playwright 模拟真实浏览器；② 低频采集（30分钟+）；③ 准备降级方案（暂停某渠道） |
| **LLM 成本** | 每个岗位评分都调 LLM 费用高 | ① 向量粗筛先过滤 90% 不相关岗位；② 只对 Top N 候选调 LLM 评分；③ 使用 gpt-4o-mini 降低成本 |
| **数据膨胀** | 长期运行岗位数据持续增长 | ① 过期岗位（发布 >30 天）归档/删除；② 定期清理匹配缓存 |
| **服务稳定性** | 爬虫崩溃导致采集中断 | ① 每个渠道独立进程/线程；② 失败自动重试 + 告警；③ Docker 自动重启 |

### 7.3 成本估算

| 项目 | 月费用估算 | 说明 |
|------|-----------|------|
| 服务器 | ¥0（本地运行）或 ¥50-100（云服务器 2C4G） | 个人使用本地即可 |
| LLM API | ¥30-80 | gpt-4o-mini 每月约 5 万次调用 |
| 域名 | ¥0 | 本地使用 localhost |
| **总计** | **¥30-80/月** | 本地部署最低成本 |

---

## 八、验收标准

### 8.1 功能验收

| 编号 | 验收项 | 通过标准 |
|------|--------|---------|
| V1 | 多渠道采集 | 真实模式下至少 6/8 渠道正常采集，日均新增岗位 ≥500；Mock 不计数 |
| V2 | 增量检测 | 重复岗位不重复入库，去重率 ≥80% |
| V3 | 字段标准化 | 学历/城市/薪资/专业 字段标准化率 ≥95% |
| V4 | 简历解析 | 上传 PDF/DOCX → 正确提取结构化画像 |
| V5 | 匹配评分 | 匹配评分与人工判断一致率 ≥70% |
| V6 | 推送通知 | 高匹配岗位在 10 分钟内推送到指定渠道 |
| V7 | 筛选查询 | 前端支持 ≥6 个维度筛选，响应 <500ms |
| V8 | 定时调度 | 各渠道按配置时间自动执行，无人工干预 |

### 8.2 性能验收

| 指标 | 目标值 |
|------|--------|
| 岗位列表查询响应 | < 500ms（1 万条数据） |
| 单次匹配计算耗时 | < 3 秒（含 LLM 调用） |
| 单渠道采集耗时 | < 60 秒 |
| 日均处理岗位量 | ≥ 2000 条 |
| 系统可用率 | ≥ 99%（7×24 运行） |

---

## 九、附录

### 9.1 关键 Python 依赖

```
# requirements.txt
fastapi==0.115.*
uvicorn[standard]==0.30.*
scrapy==2.11.*
playwright==1.45.*
httpx==0.27.*
fake-useragent==1.5.*
apscheduler==3.10.*
sqlalchemy==2.0.*
alembic==1.13.*
pdfplumber==0.11.*
python-docx==1.1.*
openai==1.40.*
chromadb==0.5.*
pydantic==2.8.*
python-multipart==0.0.*
pytest==8.*
```

### 9.2 前端依赖

```json
{
  "dependencies": {
    "vue": "^3.4",
    "vue-router": "^4.3",
    "element-plus": "^2.7",
    "ag-grid-vue3": "^31.0",
    "axios": "^1.7",
    "echarts": "^5.5",
    "pinia": "^2.1"
  }
}
```

### 9.3 反爬策略对照表

| 渠道 | 反爬等级 | 主要手段 | 应对方案 |
|------|---------|---------|---------|
| 国资委 | ★☆☆ | 无明显反爬 | 普通 httpx 请求 |
| 人社部 | ★☆☆ | 无明显反爬 | 普通 httpx 请求 |
| 企业官网 | ★★☆ | 部分有频率限制 | 低频 + UA 轮换 |
| 国聘网 | ★★☆ | Cookie 验证 | 自动获取/刷新 Cookie |
| 前程无忧 | ★★☆ | 编码混淆 | GBK 解码处理 |
| 智联招聘 | ★★☆ | Token 验证 | API 逆向 + Token 刷新 |
| Boss 直聘 | ★★★ | 指纹检测 + CAPTCHA | Playwright + 人工辅助验证 |
| 微信公众号 | ★★☆ | 频率限制 | 第三方 API 或低频采集 |

---

> **文档结束**
> 本任务书为 v1.0 初版，实施过程中可根据实际情况迭代调整。
> 建议从 **第一阶段 MVP** 开始，先跑通核心闭环再逐步扩展。
