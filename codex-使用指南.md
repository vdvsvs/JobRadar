# Codex 客户端使用指南 — JobRadar 项目

> 适用：**Codex 图形客户端**（非 Codex CLI）

---

## 📂 项目文件速查表

```
job-radar/
├── AGENTS.md                              ← 客户端打开项目目录后自动加载
├── 全网岗位监控系统-任务书.md               ← 完整设计文档（查阅用）
├── codex-使用指南.md                       ← 本文件（先看这个）
├── ⭐ 0-快速开始.md                        ← 【推荐】第一次使用先打开此文件
└── prompts/
    ├── 1-阶段一-MVP.md                     ← 第一阶段提示词
    ├── 2-阶段二-AI匹配与推送.md             ← 第二阶段提示词
    ├── 3-阶段三-全渠道与前端.md             ← 第三阶段提示词
    └── 4-阶段四-稳定性与运维.md             ← 第四阶段提示词
```

---

## 🚀 使用流程（总览）

```
┌─────────────────────────────────────────────────────────┐
│  第一步：Codex 客户端打开项目                              │
│  File → Open Folder → 选择 job-radar/ 目录                │
│  客户端会自动读取 AGENTS.md 中的项目规范                  │
└────────────────────────────┬────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────┐
│  第二步：按阶段顺序执行                                    │
│  打开 ⭐0-快速开始.md 按说明操作                            │
│  或直接打开 prompts/ 下的阶段文件 → 全部内容复制 → 粘贴    │
│  到 Codex 客户端对话框 → 发送                              │
└────────────────────────────┬────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────┐
│  第三步：阶段完成后验证                                    │
│  每个阶段底部有验收标准，对照检查是否全部通过              │
│  通过后再进入下一阶段                                     │
└────────────────────────────┬────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────┐
│  第四步：迭代修正                                         │
│  生成的代码有问题 → 在客户端直接追加修正指令               │
│  （例：「job51_crawler.py 返回乱码，请用 GBK 解码」）       │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 客户端操作详细步骤

### Step 1 — 打开项目

1. 打开 **Codex 客户端**
2. 菜单选择 **File → Open Folder**（或「文件 → 打开文件夹」）
3. 选择 `job-radar/` 文件夹
4. ✅ 确认左侧文件树可以看到 `AGENTS.md`

> 💡 **AGENTS.md 的作用**：Codex 客户端打开目录时会自动读取该文件，获取项目的技术栈、目录结构、编码规范等上下文信息。**无需你手动将 AGENTS.md 内容粘贴到对话框**。

---

### Step 2 — 执行第一阶段

#### 方法 A（推荐）：打开 `⭐0-快速开始.md`，按指引操作

#### 方法 B：直接复制阶段文件

1. 在客户端左侧文件树中，双击打开 `prompts/1-阶段一-MVP.md`
2. `Ctrl + A` 全选文件内容
3. `Ctrl + C` 复制
4. 点击客户端底部对话框
5. `Ctrl + V` 粘贴
6. 按 **Enter** 或点击「发送」按钮
7. ⏳ 等待 Codex 生成代码（通常 1-5 分钟）

---

### Step 3 — 生成完成后操作

Codex 生成完成后：

1. **检查文件是否创建**：看左侧文件树，应有以下新文件
   ```
   requirements.txt
   .env.example
   config/settings.py
   config/sources.yaml
   database/models.py
   database/session.py
   crawlers/...
   pipeline/...
   api/...
   web/...
   docker-compose.yml
   Dockerfile
   ```

2. **手动执行**：在终端（PowerShell/CMD/Terminal）中运行
   ```bash
   cd job-radar
   pip install -r requirements.txt
   cp .env.example .env    # Windows: copy .env.example .env
   python -c "from database.seed import init_db; init_db()"
   uvicorn api.main:app --reload --port 8000
   ```

3. **对照底部验收标准**逐条验证，全部通过后再继续。

---

### Step 4 — 继续后续阶段

第一阶段通过后，重复 Step 2：
- 打开 `prompts/2-阶段二-AI匹配与推送.md` → 复制 → 粘贴 → 发送
- 验证通过 → `prompts/3-阶段三-全渠道与前端.md`
- 验证通过 → `prompts/4-阶段四-稳定性与运维.md`

---

## 💬 与 Codex 客户端的对话技巧

### 1. 遇到错误时的修正指令

直接在对话框继续输入（**不要重新发整个阶段文件**）：

```
上面生成的 job51_crawler.py 运行时报错：
AttributeError: 'NoneType' object has no attribute 'text'
请修复选择器逻辑，增加 None 判断。
```

```
岗位列表页的城市筛选下拉没有选项。
请检查 api/routers/jobs.py 中是否有返回城市列表的接口，
如果没有请添加一个 GET /api/jobs/filters 接口，
返回所有可选的城市、学历、来源列表。
前端 JobList.vue 也要对应调用这个接口填充筛选项。
```

### 2. 需要生成更多内容时

```
请继续完善 Task 3.3 企业官网爬虫，
新增 10 个互联网大厂的具体 parser 实现：
腾讯、阿里、字节跳动、百度、美团、京东、拼多多、快手、网易、滴滴。
每个 parser 用注释说明假设的页面结构。
```

### 3. 缩短回复长度时

```
请只完成 Task 1.1 ~ Task 1.3，
剩余 Task 我在下一轮对话中再要求生成。
```

---

## ⚠️ 常见问题

### Q: Codex 客户端回复被截断怎么办？

在对话框输入：
```
请继续刚才的生成，从被截断的 Task X.X 开始。
```

### Q: 生成的爬虫无法抓取真实数据？

在对话框追加修正：
```
iguopin_crawler.py 目前是 Mock 版本。
我抓包发现国聘网的真实搜索接口是：
POST https://www.iguopin.com/api/job/search
请求体: {page: 1, pageSize: 20, keyword: ""}
响应格式: {code: 0, data: {list: [{jobName, companyName, ...}]}}
请根据真实接口重写爬虫。
```

### Q: Boss 直聘反爬太严？

```
Boss 直聘爬虫遇到滑块验证时，请改为：
1. Playwright 用 headless=False 启动
2. 检测到验证码元素时，打印提示并 sleep(120)
3. 给我 2 分钟在弹出的浏览器中手动完成验证
4. 验证通过后继续采集
5. 采集完成后把浏览器 context 保存到 data/boss_context/
6. 下次采集优先加载保存的 context
```

### Q: LLM 调用超时？

```
请优化 matching/llm_scorer.py：
1. 添加 httpx.AsyncClient(timeout=60.0)
2. 增加 try/except 捕获 TimeoutException
3. 超时后自动重试 2 次
4. 重试都失败时降级为纯向量相似度 * 5 作为粗评分
```

---

## ✅ 最终验收清单

全部 4 阶段完成后，逐项勾选：

### 基础设施
- [ ] `docker compose up -d` 可以一键启动所有容器
- [ ] `GET /api/health` 返回 healthy
- [ ] Docker 异常退出后自动重启

### 采集引擎
- [ ] 8 个渠道爬虫已注册
- [ ] 至少 6/8 渠道能采集真实数据
- [ ] 采集日志有记录
- [ ] 某渠道失败时不影响其他渠道

### 数据处理
- [ ] 去重有效（同岗不同源不重复入库）
- [ ] 学历/城市/薪资字段标准化
- [ ] 岗位数据可通过 API 多维筛选（< 500ms）

### AI 匹配
- [ ] 上传 PDF/DOCX 可解析出结构化画像
- [ ] 匹配返回 Top N 岗位，含评分和原因
- [ ] 新岗位入库后自动触发增量匹配
- [ ] ≥4.0 分的岗位推送通知（邮件/微信至少一个通）

### 前端
- [ ] 岗位列表页（AG Grid + 筛选 + 分页）
- [ ] 岗位详情页（匹配分条形图）
- [ ] 简历上传页（画像展示）
- [ ] 匹配结果页（按匹配分降序）
- [ ] 采集看板（渠道状态 + 图表）
- [ ] 通知中心 + 实时弹窗

### 运维
- [ ] 日志按天轮转
- [ ] 数据每日自动备份
- [ ] 30 天前岗位自动归档
- [ ] README.md 有部署说明
